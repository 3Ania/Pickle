package com.example.greetingcard.ui.theme

import androidx.compose.ui.graphics.Color

val PickleGreen = Color(0xFF1B5E20)        // Główny "ogórkowy" zielony (przyciski)
val PickleLightBackground = Color(0xFFF1F8E9) // Bardzo jasne, zielonkawe tło
val PickleSurface = Color(0xFFDCEDC8)      // Nieco ciemniejsze tła (okienka, menu)
val DillYellow = Color(0xFF5D9D5E)         // Akcent "koperkowy" żółty (przyciski poboczne)

val TextDarkGreen = Color(0xFF1B5E20)      // Bardzo ciemny zielony na teksty (zamiast czarnego)
val TextWhite = Color(0xFFFFFFFF)          // Biały tekst na główne przyciski
